% Language and encoding
\usepackage[utf8]{inputenc}
\usepackage[LGR,T1]{fontenc}
\usepackage[ngerman,english]{babel}

% Basic utilities
\usepackage{xpatch}
\usepackage{etoolbox}
\usepackage{calc}
\usepackage{relsize}

% Page layout -- Gerhardt/Bonn 2026, 11 pt version
\newlength{\baselinedist}
\newlength{\smalllinespacing}
\newlength{\footnotelinespacing}
\setlength{\baselinedist}{19.5pt}
\setlength{\smalllinespacing}{15pt}
\setlength{\footnotelinespacing}{13.5pt}

\usepackage[
  bottom=2.8cm,
  left=2.5cm,
  right=4cm,
  top=2.8cm,
  headheight=15pt,
  headsep=22.5pt,
  marginpar=3.25cm,
  footnotesep=19.5pt plus 39pt
]{geometry}

\let\LargeOrig\Large
\let\largeOrig\large
\let\normalsizeOrig\normalsize
\let\smallOrig\small
\let\footnotesizeOrig\footnotesize

\renewcommand{\normalsize}{\normalsizeOrig\fontsize{11pt}{19.5pt}\selectfont}
\renewcommand{\Large}{\LargeOrig\fontsize{14pt}{19.5pt}\selectfont}
\renewcommand{\large}{\largeOrig\fontsize{12pt}{19.5pt}\selectfont}
\renewcommand{\small}{\smallOrig\fontsize{10pt}{15pt}\selectfont}
\renewcommand{\footnotesize}{\footnotesizeOrig\fontsize{9pt}{13.5pt}\selectfont}
\normalsize

\setlength{\textheight}{35\baselinedist}
\setlength{\skip\footins}{19.5pt plus 58.5pt}

\AtBeginDocument{%
  \setlength{\baselineskip}{\baselinedist}%
  \setlength{\topskip}{\baselineskip}%
  \setlength{\parindent}{\baselineskip}%
  \setlength{\parskip}{0pt}%
  \frenchspacing
  \let\nonfrenchspacing=\frenchspacing
  \sloppy
}

% Fonts -- same Times-style setup as Gerhardt template
\usepackage[scale=0.995,shrink=0.025em,stretch=.15em]{newtxtext}
\usepackage[scale=0.995,slantedGreek]{newtxmath}
\usepackage[book,lining,scale=0.85,semibold,tabular]{FiraSans}
\usepackage[lining,scale=0.85]{FiraMono}
\usepackage[protrusion=compatibility,expansion=false]{microtype}

% Mathematics
% Do NOT load amssymb separately: newtxmath already provides the needed symbols.
\usepackage{amsmath,mathtools}
\usepackage{bm}
\numberwithin{equation}{section}

% Tables and figures
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{makecell}
\usepackage{tabularx}
\usepackage{array}

% Bonn colors and clickable PDF links
\usepackage[svgnames,x11names]{xcolor}
\definecolor{UBonnBlueDark}{RGB}{5,61,115}

\PassOptionsToPackage{hyphens}{url}
\usepackage[unicode]{hyperref}

\hypersetup{
  allcolors=UBonnBlueDark,
  colorlinks=true,
  pdftitle={Beliefs about Refugee Integration in Germany: A Survey Experiment on Ukrainian and Syrian Refugees},
  pdfauthor={Lidiia Levytska},
  bookmarksnumbered=true
}

\newcommand*{\Appendixautorefname}{Appendix}

% Footnotes
\usepackage[bottom,multiple,splitrule,stable]{footmisc}

% Running header -- same structure as friend's Gerhardt template
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}

\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

\fancyhead[L]{%
  \sffamily\small\relscale{0.95}\strut
  Lidiia Levytska: \textit{Beliefs about Refugee Integration in Germany}%
}

\fancyhead[R]{%
  \sffamily\small\relscale{0.95}\strut
  \thepage%
}

% Section headings
\usepackage[explicit]{titlesec}
\setcounter{secnumdepth}{4}

\titleformat{\section}
  {\normalfont\Large\bfseries}
  {\strut\thesection}
  {1em}
  {#1\strut}

\titlespacing*{\section}
  {0pt}
  {2\baselinedist plus 4ex}
  {1\baselinedist plus 2ex}

\titleformat{\subsection}
  {\normalfont\large\bfseries}
  {\strut\thesubsection}
  {1em}
  {#1\strut}

\titlespacing*{\subsection}
  {0pt}
  {1\baselinedist plus 2ex}
  {1\baselinedist plus 2ex}

\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries}
  {\strut\thesubsubsection}
  {1em}
  {#1\strut}

\titlespacing*{\subsubsection}
  {0pt}
  {0.5\baselinedist plus 1ex}
  {0.5\baselinedist plus 1ex}

% Captions
\usepackage[
  font=small,
  labelfont=bf,
  labelsep=period
]{caption}

% Bibliography -- Chicago author-date
% Existing \citet and \citep commands remain supported.
\usepackage[
  authordate,
  backend=biber,
  compresspages=false,
  dashed=false,
  doi=only,
  isbn=false,
  maxbibnames=6,
  minbibnames=3,
  maxcitenames=2,
  mincitenames=1,
  natbib=true,
  noibid,
  sortcites=true,
  sorting=ynt,
  useprefix=true
]{biblatex-chicago}

\usepackage[babel,german=quotes]{csquotes}

\addbibresource{references.bib}

\urlstyle{same}

\setcounter{biburllcpenalty}{10000}
\setcounter{biburlucpenalty}{10000}
\setcounter{biburlnumpenalty}{10000}

\renewcommand{\bibfont}{\small}
\setlength{\bibitemsep}{0pt}

% Appendix formatting -- A-1, A-2, ... as in friend's thesis
\usepackage{appendix}
\usepackage{chngcntr}

\AtBeginEnvironment{appendix}{%
  \clearpage
  \small
  \counterwithin{equation}{section}%
  \counterwithin{figure}{section}%
  \counterwithin{table}{section}%
  \renewcommand{\thepage}{A-\arabic{page}}%
  \setcounter{page}{1}%
  \titleformat{\section}
    {\normalfont\Large\bfseries}
    {\strut\appendixname~\thesection}
    {1em}
    {#1\strut}%
}